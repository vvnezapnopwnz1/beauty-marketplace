# План реализации: блок «Бизнес» — 4 раздела для Мастера (mobile)

> **Исполнитель-агент:** прочитай `docs/vault/README.md` и `docs/vault/product/status.md` перед стартом.
> Работаем только в `mobile/` и `backend/`. Не трогаем frontend/.

---

## Контекст

В мобильном приложении вкладка «Бизнес» (`MoreScreen`) содержит BentoCard-плитки и QuickActionsRow.
Сейчас только плитка «Услуги» имеет навигацию, но возвращает 400.
Остальные плитки (График, Финансы, Уведомления) никуда не ведут.

Цель: реализовать 4 полноценных раздела Мастера и проставить навигацию со всех плиток.

---

## Задача 0 — Проставить навигацию на BentoCards (5 мин)

Файл: `mobile/src/features/more/MoreScreen.tsx`

Добавить `onPress` на все BentoCard-плитки:

| Плитка | Маршрут |
|--------|---------|
| ГРАФИК | `/(settings)/schedule` |
| ФИНАНСЫ | `/(settings)/finances` |
| УВЕДОМЛЕНИЯ | `/(settings)/notifications` |
| УСЛУГИ | уже есть `/(settings)/services` |
| САЛОНЫ | оставить без onPress (пока) |

---

## Задача 1 — Услуги: улучшенный список + редактирование

### 1.1 Исправить 400 ошибку

Проверить в логах бэкенда и на мобильном клиенте:
- Если пользователь не имеет MasterProfile → контроллер вернёт 403, а не 400. Выяснить реальный код.
- Убедиться, что `MASTER.services = \`${API_V1}/master-dashboard/services\`` попадает на маршрут `mux.HandleFunc("/api/v1/master-dashboard/", ...)`.
- Если проблема в auth-токене — проверить `apiClient` interceptor.

### 1.2 Обновить тип `MasterService` в `mobile/src/entities/services/api.ts`

Добавить поле:
```ts
description?: string | null;
isActive: boolean;
```

### 1.3 Добавить `useUpdateMasterServiceMutation` в `mobile/src/entities/services/api.ts`

```ts
export function useUpdateMasterServiceMutation() {
  const qc = useQueryClient();
  return useMutation({
    mutationFn: async ({ id, ...input }: { id: string } & CreateMasterServiceInput) => {
      const { data } = await apiClient.put<MasterService>(MASTER.service(id), input);
      return data;
    },
    onSuccess: () => {
      void qc.invalidateQueries({ queryKey: ["services"] });
    },
  });
}
```

Endpoint `MASTER.service(id)` уже есть в `endpoints.ts`: `${API_V1}/master-dashboard/services/${id}`.

### 1.4 Создать `mobile/app/services/[id]/edit.tsx`

Экран редактирования услуги. Аналогичен `services/new.tsx`, но:
- Берёт `id` из `useLocalSearchParams()`
- Предзаполняет поля из `useMasterServicesQuery()` (найти по id)
- Использует `useUpdateMasterServiceMutation()` вместо create
- Кнопка «Сохранить» → `PUT /api/v1/master-dashboard/services/:id`
- Добавить переключатель «Активна» (isActive) — чекбокс/toggle

### 1.5 Обновить `mobile/app/(settings)/services.tsx`

- **Категорийные чипы (фильтр)**: список уникальных `categorySlug` из данных + «Все». Горизонтальный ScrollView с чип-кнопками.
- **Карточка услуги** — расширить:
  - Название, цена, длительность
  - Категория
  - Описание (1 строка, truncate)
  - Бейдж «Неактивна» если `isActive === false`
  - Кнопка-иконка «Карандаш» → `router.push(\`/services/${service.id}/edit\`)`
- **FAB «+»** в правом нижнем углу → `router.push("/services/new")`
- Логика фильтрации на клиенте (не новый API-запрос)

---

## Задача 2 — Уведомления: чтение, пометка прочитанным

### 2.1 Исправить несоответствие API-ответа

Бэкенд возвращает `{ "items": [...] }` (объект), а мобильный клиент ожидает `InboxNotification[]` (массив).

**Исправить в `mobile/src/entities/notifications/api.ts`:**

```ts
export function useNotificationsQuery(limit = 30, offset = 0) {
  return useQuery({
    queryKey: ["notifications", { limit, offset }],
    queryFn: async () => {
      const query = new URLSearchParams({ limit: String(limit), offset: String(offset) });
      const { data } = await apiClient.get<{ items: InboxNotification[] }>(
        `${NOTIFICATIONS.list}?${query.toString()}`
      );
      return data.items ?? [];
    },
    refetchInterval: 30_000,
  });
}
```

### 2.2 Добавить мутации в `mobile/src/entities/notifications/api.ts`

```ts
export function useMarkNotificationReadMutation() {
  const qc = useQueryClient();
  return useMutation({
    mutationFn: async (id: string) => {
      await apiClient.post(NOTIFICATIONS.markRead(id));
    },
    onSuccess: () => {
      void qc.invalidateQueries({ queryKey: ["notifications"] });
      void qc.invalidateQueries({ queryKey: ["notificationsUnreadCount"] });
    },
  });
}

export function useMarkAllReadMutation() {
  const qc = useQueryClient();
  return useMutation({
    mutationFn: async () => {
      await apiClient.post(NOTIFICATIONS.markAllRead);
    },
    onSuccess: () => {
      void qc.invalidateQueries({ queryKey: ["notifications"] });
      void qc.invalidateQueries({ queryKey: ["notificationsUnreadCount"] });
    },
  });
}
```

Endpoints уже есть в `NOTIFICATIONS`: `markRead(id)`, `markAllRead`, `markSeen(id)`, `markAllSeen`.

### 2.3 Обновить `mobile/app/(settings)/notifications.tsx`

- **Фильтр-табы**: «Все» / «Непрочитанные» (передавать `unread=true` в query)
- **Карточка уведомления**:
  - Левая полоса accent-цвета если `!isRead`
  - Title (bold если !isRead), body, дата
  - Tap на карточку → вызывает `markRead(id)`, затем expandable детальный просмотр (expand in-place)
- **Кнопка «Прочитать все»** в header (если есть непрочитанные) → `markAllRead()`
- Не нужен отдельный экран «деталей» — достаточно expand в списке (toggle `expanded` state)

---

## Задача 3 — Финансы: полноценная мобильная версия

### 3.1 Расширить `mobile/src/entities/finances/api.ts`

Добавить типы и хуки, аналогичные `frontend/src/entities/master-finances/model/masterFinancesApi.ts`:

**Типы:**
```ts
export type FinanceTrendPoint = {
  date: string;
  incomeCents: number;
  expenseCents: number;
};

export type TopService = {
  serviceName: string;
  incomeCents: number;
};

export type ExpenseCategory = {
  id: string;
  name: string;
  emoji: string;
  sortOrder: number;
};

export type Expense = {
  id: string;
  categoryId?: string | null;
  categoryName?: string | null;
  amountCents: number;
  description: string;
  expenseDate: string;
  createdAt: string;
};

export type ExpenseListResponse = {
  items: Expense[];
  total: number;
};
```

**Добавить в `MASTER` endpoints (endpoints.ts):**
```ts
financesTrend: `${API_V1}/master-dashboard/finances/trends`,
financesTopServices: `${API_V1}/master-dashboard/finances/top-services`,
financesCategories: `${API_V1}/master-dashboard/finances/categories`,
financesCategory: (id: string) => `${API_V1}/master-dashboard/finances/categories/${id}`,
financesExpenses: `${API_V1}/master-dashboard/finances/expenses`,
financesExpense: (id: string) => `${API_V1}/master-dashboard/finances/expenses/${id}`,
```

> Примечание: `financesSummary`, `financesExpenseCategories`, `financesExpenses` уже есть в endpoints.ts — используй их, обновив имена при необходимости.

**Хуки (добавить в finances/api.ts):**
```ts
export function useFinanceSummaryQuery(params: { source?: string; from?: string; to?: string }) { ... }
export function useFinanceTrendQuery(params: { source?: string; from?: string; to?: string }) { ... }
export function useTopServicesQuery(params: { source?: string; from?: string; to?: string }) { ... }
export function useExpenseCategoriesQuery() { ... }
export function useCreateExpenseCategoryMutation() { ... }
export function useDeleteExpenseCategoryMutation() { ... }
export function useExpensesQuery(params: { from?: string; to?: string; page?: number; pageSize?: number }) { ... }
export function useCreateExpenseMutation() { ... }
export function useDeleteExpenseMutation() { ... }
```

### 3.2 Переписать `mobile/app/(settings)/finances.tsx`

Используй состояние: `from` (string YYYY-MM-DD), `to`, `source` (all/personal/salon).

**Секции экрана (ScrollView):**

1. **Фильтр-бар** (sticky внутри ScrollView):
   - Три кнопки-чипа: Все / Личные / Салонные
   - Две кнопки выбора дат (from / to) — tap открывает нативный `DatePickerAndroid` / модальный `DateTimePicker` (`@react-native-community/datetimepicker`)
   - Кнопка «Добавить расход» → открыть bottom sheet

2. **Сводка** (3 карточки в ряд или стопкой):
   - Доход / Расходы / Прибыль — данные из `useFinanceSummaryQuery`

3. **Тренд** (мини-линейный график):
   - Использовать библиотеку `react-native-gifted-charts` или `victory-native` (проверь что установлено в package.json, иначе используй SVG полоски без библиотеки)
   - Ось X — даты, два ряда: доход (зелёный) и расход (красный)
   - Если графическая библиотека отсутствует — показать простую таблицу дат с цифрами

4. **Топ услуг** — горизонтальные полоски (View с процентным fill), без библиотеки charts

5. **Расходы** — FlatList:
   - Дата, категория (emoji + название), сумма, описание
   - Свайп влево (или кнопка «×») → удалить расход

6. **Bottom Sheet «Новый расход»** (использовать `@gorhom/bottom-sheet` если установлено, иначе Modal):
   - Поля: Сумма (₽), Дата, Категория (Select/Picker), Описание
   - Кнопка «Сохранить»

7. **Категории расходов** — отдельная секция в нижней части или вкладка:
   - Список категорий с emoji и названием
   - Кнопка «×» для удаления
   - Поле «+ Добавить категорию» (name + emoji)

---

## Задача 4 — Расписание: индивидуальное расписание Мастера

### 4.1 Backend: новая таблица и API

> Файлы для изменения — только в `backend/`.

#### 4.1.1 Модель `MasterWorkingHour` в `backend/internal/infrastructure/persistence/model/models.go`

Добавить после `MasterService`:

```go
// MasterWorkingHour is the master's personal working hours (independent from any salon).
// dayOfWeek: 0=Mon, 1=Tue, ..., 6=Sun (ISO week order)
type MasterWorkingHour struct {
	ID              uuid.UUID `gorm:"type:uuid;primaryKey;default:gen_random_uuid()"`
	MasterProfileID uuid.UUID `gorm:"type:uuid;not null;column:master_profile_id;index"`
	DayOfWeek       int       `gorm:"column:day_of_week;not null"` // 0-6
	OpensAt         string    `gorm:"column:opens_at;type:varchar(5)"` // "09:00"
	ClosesAt        string    `gorm:"column:closes_at;type:varchar(5)"` // "20:00"
	IsClosed        bool      `gorm:"column:is_closed;not null;default:false"`
}

func (MasterWorkingHour) TableName() string { return "master_working_hours" }

func (h *MasterWorkingHour) BeforeCreate(tx *gorm.DB) error {
	if h.ID == uuid.Nil {
		h.ID = uuid.New()
	}
	return nil
}
```

#### 4.1.2 Миграция таблицы

Проверить, как в проекте делаются миграции (обычно GORM AutoMigrate или SQL-файлы в `backend/migrations/`).

Если AutoMigrate — добавить `&model.MasterWorkingHour{}` в список моделей в `AutoMigrate` вызове.

Если SQL-файлы — создать новый файл `backend/migrations/NNNN_create_master_working_hours.sql`:
```sql
CREATE TABLE IF NOT EXISTS master_working_hours (
    id              UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    master_profile_id UUID NOT NULL REFERENCES master_profiles(id) ON DELETE CASCADE,
    day_of_week     SMALLINT NOT NULL CHECK (day_of_week BETWEEN 0 AND 6),
    opens_at        VARCHAR(5) NOT NULL DEFAULT '09:00',
    closes_at       VARCHAR(5) NOT NULL DEFAULT '20:00',
    is_closed       BOOLEAN NOT NULL DEFAULT false,
    UNIQUE(master_profile_id, day_of_week)
);
CREATE INDEX IF NOT EXISTS idx_mwh_master ON master_working_hours(master_profile_id);
```

#### 4.1.3 Repository в `backend/internal/repository/`

Создать файл `backend/internal/repository/master_schedule.go`:
```go
package repository

import (
    "context"
    "github.com/beauty-marketplace/backend/internal/infrastructure/persistence/model"
    "github.com/google/uuid"
)

type MasterScheduleRepository interface {
    ListMasterWorkingHours(ctx context.Context, masterProfileID uuid.UUID) ([]model.MasterWorkingHour, error)
    ReplaceMasterWorkingHours(ctx context.Context, masterProfileID uuid.UUID, rows []model.MasterWorkingHour) error
}
```

Создать реализацию в `backend/internal/infrastructure/persistence/master_schedule_repository.go`:
```go
package persistence

import (
    "context"
    "github.com/beauty-marketplace/backend/internal/infrastructure/persistence/model"
    "github.com/google/uuid"
    "gorm.io/gorm"
    "gorm.io/gorm/clause"
)

type masterScheduleRepository struct{ db *gorm.DB }

func NewMasterScheduleRepository(db *gorm.DB) *masterScheduleRepository {
    return &masterScheduleRepository{db: db}
}

func (r *masterScheduleRepository) ListMasterWorkingHours(ctx context.Context, masterProfileID uuid.UUID) ([]model.MasterWorkingHour, error) {
    var rows []model.MasterWorkingHour
    err := r.db.WithContext(ctx).
        Where("master_profile_id = ?", masterProfileID).
        Order("day_of_week").
        Find(&rows).Error
    return rows, err
}

func (r *masterScheduleRepository) ReplaceMasterWorkingHours(ctx context.Context, masterProfileID uuid.UUID, rows []model.MasterWorkingHour) error {
    return r.db.WithContext(ctx).Transaction(func(tx *gorm.DB) error {
        if err := tx.Where("master_profile_id = ?", masterProfileID).Delete(&model.MasterWorkingHour{}).Error; err != nil {
            return err
        }
        for i := range rows {
            rows[i].MasterProfileID = masterProfileID
        }
        if len(rows) == 0 {
            return nil
        }
        return tx.Clauses(clause.OnConflict{DoNothing: true}).Create(&rows).Error
    })
}
```

#### 4.1.4 Добавить метод сида в сервис

В `backend/internal/service/master_dashboard.go`, в функцию создания MasterProfile (найди `CreateMasterProfile` или аналогичный метод — или hook `AfterCreate`), добавить сид расписания по умолчанию:

```go
// defaultMasterSchedule returns Mon-Fri 09:00-20:00, Sat 10:00-18:00, Sun closed.
func defaultMasterSchedule(masterProfileID uuid.UUID) []model.MasterWorkingHour {
    type dayConfig struct {
        opens, closes string
        closed        bool
    }
    configs := []dayConfig{
        {"09:00", "20:00", false}, // Mon
        {"09:00", "20:00", false}, // Tue
        {"09:00", "20:00", false}, // Wed
        {"09:00", "20:00", false}, // Thu
        {"09:00", "20:00", false}, // Fri
        {"10:00", "18:00", false}, // Sat
        {"00:00", "00:00", true},  // Sun
    }
    rows := make([]model.MasterWorkingHour, 7)
    for i, c := range configs {
        rows[i] = model.MasterWorkingHour{
            MasterProfileID: masterProfileID,
            DayOfWeek:       i,
            OpensAt:         c.opens,
            ClosesAt:        c.closes,
            IsClosed:        c.closed,
        }
    }
    return rows
}
```

Вызвать сид после успешного создания MasterProfile:
```go
if err := s.scheduleRepo.ReplaceMasterWorkingHours(ctx, newProfile.ID, defaultMasterSchedule(newProfile.ID)); err != nil {
    s.log.Warn("failed to seed master schedule", zap.Error(err))
    // non-fatal, don't fail profile creation
}
```

#### 4.1.5 DTOs и service методы в `backend/internal/service/master_dashboard.go`

Добавить DTOs:
```go
type MasterWorkingHourDTO struct {
    DayOfWeek int    `json:"dayOfWeek"`
    OpensAt   string `json:"opensAt"`
    ClosesAt  string `json:"closesAt"`
    IsClosed  bool   `json:"isClosed"`
}

type UpdateMasterScheduleInput struct {
    Hours []MasterWorkingHourDTO `json:"hours"`
}
```

Добавить в интерфейс `MasterDashboardService`:
```go
GetMasterSchedule(ctx context.Context, userID uuid.UUID) ([]MasterWorkingHourDTO, error)
UpdateMasterSchedule(ctx context.Context, userID uuid.UUID, in UpdateMasterScheduleInput) ([]MasterWorkingHourDTO, error)
```

Реализовать методы: найти masterProfileID по userID → вызвать scheduleRepo.

Убедиться, что `masterDashboardService` получает `scheduleRepo MasterScheduleRepository` через DI и добавить в провайдер в `app.go`.

#### 4.1.6 Эндпоинты в `MasterDashboardController`

Добавить в `switch parts[0]` в `MasterDashboardRoutes`:

```go
case "schedule":
    if len(parts) == 1 && r.Method == http.MethodGet {
        out, err := h.svc.GetMasterSchedule(r.Context(), userID)
        if err != nil {
            h.log.Error("master get schedule", zap.Error(err))
            jsonError(w, "internal error", http.StatusInternalServerError)
            return
        }
        w.Header().Set("Content-Type", "application/json")
        _ = json.NewEncoder(w).Encode(out)
        return
    }
    if len(parts) == 1 && r.Method == http.MethodPut {
        var body service.UpdateMasterScheduleInput
        if err := json.NewDecoder(r.Body).Decode(&body); err != nil {
            jsonError(w, "invalid json", http.StatusBadRequest)
            return
        }
        out, err := h.svc.UpdateMasterSchedule(r.Context(), userID, body)
        if err != nil {
            jsonError(w, err.Error(), http.StatusBadRequest)
            return
        }
        w.Header().Set("Content-Type", "application/json")
        _ = json.NewEncoder(w).Encode(out)
        return
    }
    http.NotFound(w, r)
```

### 4.2 Mobile: entity и API

#### 4.2.1 Добавить эндпоинты в `mobile/src/api/endpoints.ts`

В объект `MASTER`:
```ts
schedule: `${API_V1}/master-dashboard/schedule`,
```

#### 4.2.2 Создать `mobile/src/entities/schedule/api.ts`

```ts
import { useMutation, useQuery, useQueryClient } from "@tanstack/react-query";
import apiClient from "../../api/client";
import { MASTER } from "../../api/endpoints";

export type WorkingHour = {
  dayOfWeek: number; // 0=Mon … 6=Sun
  opensAt: string;   // "09:00"
  closesAt: string;  // "20:00"
  isClosed: boolean;
};

export function useMasterScheduleQuery() {
  return useQuery({
    queryKey: ["masterSchedule"],
    queryFn: async () => {
      const { data } = await apiClient.get<WorkingHour[]>(MASTER.schedule);
      return data;
    },
  });
}

export function useUpdateMasterScheduleMutation() {
  const qc = useQueryClient();
  return useMutation({
    mutationFn: async (hours: WorkingHour[]) => {
      const { data } = await apiClient.put<WorkingHour[]>(MASTER.schedule, { hours });
      return data;
    },
    onSuccess: () => {
      void qc.invalidateQueries({ queryKey: ["masterSchedule"] });
    },
  });
}
```

### 4.3 Переписать `mobile/app/(settings)/schedule.tsx`

Заменить «Расписание салона» на «Расписание мастера».

**Дизайн экрана:**

- Заголовок: «Моё расписание» + подпись «Личное, не зависит от салона»
- 7 карточек (Пн–Вс), каждая:
  - Название дня + переключатель «Выходной» (Toggle/Switch)
  - Если НЕ выходной: два поля времени «С» / «По» (tap → TimePicker или TextInput с маской `HH:MM`)
- Кнопка «Сохранить» в нижней части (sticky или в header)
- При сохранении вызывает `useUpdateMasterScheduleMutation` с массивом 7 объектов
- На экране показывать состояние редактирования локально (controlled state), сохранять только по кнопке

**Формат дней:**
```ts
const DAY_NAMES = ["Пн", "Вт", "Ср", "Чт", "Пт", "Сб", "Вс"];
```

---

## Порядок выполнения

Рекомендуемая последовательность для минимального риска:

1. **Задача 0** — BentoCards навигация (5 мин, не ломает ничего)
2. **Задача 1.1** — Диагностика 400 ошибки (исследование)
3. **Задача 2.1** — Fix notification API response mismatch (критичный баг)
4. **Задача 4.1** — Backend: schedule (самое долгое, требует Go)
5. **Задача 1.2–1.5** — Services улучшения
6. **Задача 2.2–2.3** — Notifications улучшения
7. **Задача 3** — Finances (самое объёмное с UI-стороны)
8. **Задача 4.2–4.3** — Mobile schedule UI

---

## Команды проверки (запускать в конце)

```bash
# Backend
cd backend && go build ./... && go test ./internal/service/... -run TestMaster
# Mobile (проверка TS)
cd mobile && npx tsc --noEmit
```

---

## Что НЕ делать

- Не трогать салонный дашборд (dashboard_controller.go)
- Не менять frontend/ (это отдельный проект)
- Не добавлять графические библиотеки без проверки `package.json`
- Не коммитить, пока пользователь не попросит
