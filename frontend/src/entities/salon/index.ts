export type { Salon, Service, BusinessType, CategoryId, SalonBadge, CardGradient } from './model/types'
export { mockSalons, CATEGORIES, formatPrice, minPrice, CARD_GRADIENTS } from './model/mockData'
export { SalonCard } from './ui/SalonCard'
