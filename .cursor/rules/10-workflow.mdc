---
description: Implementation workflow and done criteria
alwaysApply: true
---

# Workflow

For coding tasks:

0. If the task touches product behavior, APIs, schema, or dashboard flows: skim **`docs/vault/README.md`** and **`docs/vault/product/status.md`** (same bootstrap as `CLAUDE.md` / Claude Code).
1. Read impacted code and relevant vault notes (`architecture/code-map.md`, `entities/*`, `runbooks/*` as needed).
2. Implement minimal focused change.
3. Run relevant checks:
   - backend: `go test ./...`
   - frontend: `npm run lint`
   - frontend build-sensitive: `npm run build`
4. Report what was changed, what was verified, and any remaining risk.

# Git Safety

- Do not commit unless user explicitly asks.
- Do not push unless user explicitly asks.
- Do not use destructive git commands without explicit request.
