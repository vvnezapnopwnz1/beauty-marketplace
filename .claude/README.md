# Claude Workspace Notes

Primary instructions live in:

- `CLAUDE.md`
- `AGENTS.md`

Keep this folder for optional Claude-specific presets (commands, templates) if needed later.
Avoid duplicating rules that already exist in `AGENTS.md`.
